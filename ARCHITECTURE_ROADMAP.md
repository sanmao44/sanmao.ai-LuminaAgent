# SANMAO.AI 架构演进路线（基于 0.7.49 现状）

> 用途：把“巨型文件、工具运行时、任务底座”这类架构话题，落成有先后顺序、能验证的改造清单。
> 铁律不变：平时只同步不发布，细节见 `WORKFLOW.md`。

## 一、第三方建议的取舍

| 建议 | 判断 | 依据 |
| --- | --- | --- |
| 抽出工具统一执行点（Tool Runtime） | 该做，已落地 | 原先 `app/api/agent/route.ts` 按工具名写 if 链，加工具要改多处，模型编造的工具名还会掉进打包/装技能的兜底分支 |
| 抽出任务底座（Job Runtime） | 该做，已落地 | 视频与高清任务各写一套读写、分页、状态流转，且都不能取消或重试 |
| 产物（Artifact）体系 | 已经有了，不必新建 | `lib/artifacts/` 已含 14 个模块（预览、导出、打包），只需接进新的执行器 |
| 先拆 `app/page.tsx` / `SuperCanvas.tsx` 巨型文件 | 不能先做 | 25 个测试读 `app/page.tsx`、31 个读 `SuperCanvas.tsx` 做源码字符串断言；先拆文件会让大量测试跟着返工 |
| MCP / 外部工具协议接入 | 可以后做 | 有了注册表与执行器，接入 MCP 等于多一个标签的 handler |
| 知识库 / RAG | 最后再评估 | 与当前主流程耦合低，收益不确定 |

`app/page.tsx` 约 1.7 万行、`components/SuperCanvas.tsx` 约 2.1 万行，是全项目最大的两个文件；拆分前要先把依赖源码字符串的断言改成行为断言。

## 二、已落地

1. 工具权限执行点 `lib/tools/policy.ts`（`resolveToolPolicy`）：native 与 MCP 走同一道判断，未知工具、本轮没下发的工具、没开写入的 MCP 写工具都会被拒绝，并把原因回给模型。`lib/tools/executor.ts` 只把注册表标签（web / file / artifact / image / skill / mcp）映射成 route 里的执行分支，分派不再看工具名。
2. 任务底座 `lib/task-store.ts`：`createTaskStore` 统一读写、串行化写入 + 临时文件 + 原子替换，视频与高清任务存储都基于它重写；接口与磁盘格式保持兼容。
3. 取消 / 重试：视频、高清放大两条云端任务链路都支持 `PATCH { action: 'cancel' | 'retry' }`，状态新增 `cancelled`，界面（创作记录卡片、视频工作台、生图任务卡）给出“停止跟踪 / 重试”入口，取消与失败分开记账。

主界面与超级画布的 Agent 面板都走 `/api/agent`（`lib/agent-client.ts`、`lib/creation/agent.ts`），执行点与任务改动对两者同时生效。

## 三、下一步建议顺序

1. 测试改造：把读源码字符串的断言逐步换成行为断言（先做 `SuperCanvas.tsx` 的 31 个），之后拆文件才安全。
2. 任务事件流：给任务底座补统一的进度 / 日志回调，让画布、创作记录、通知共用一份状态。
3. 外部工具扩展：接入新的 MCP 服务或插件时只加标签与权限声明，route 的执行分支不用改。
4. 知识库 / RAG 放到最后，先确认真实需求再做。

## 四、已知边界

- 云端任务取消只停止本地轮询，服务商侧不会撤销，可能继续计费；界面文案已提示。
- 本次改动之前创建的高清任务没有保存原图引用，重试会提示重新发起超分。
