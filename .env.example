# 本地运行不是必填。SANMAO.AI 会自动生成 .data/master.key。
# 公开部署时建议设置为稳定的高强度随机字符串。
# SANMAO_MASTER_KEY=your-long-random-secret

# 数据目录；不设置时默认使用项目内 .data
# SANMAO_DATA_DIR=.data

# 图片存储目录；不设置时默认使用 SANMAO_DATA_DIR/images
# SANMAO_IMAGE_STORAGE_PATH=.data/images

# 普通用户无需配置。发布版本可内置项目的临时媒体中转地址；本变量用于覆盖默认值或自托管客户端。
# 中转服务只保存图片约 30 分钟，不能填 localhost、局域网 IP 或普通 HTTP 地址。
# SANMAO_MEDIA_RELAY_URL=https://media.your-project.example.com
# 发布版由项目方把正式地址内置为默认值；本变量仅供开发/测试覆盖默认地址。
# SANMAO_DEFAULT_MEDIA_RELAY_URL=https://media.your-project.example.com
# 高级自托管且 relay 开启上传令牌时，客户端需要配置同一个令牌；默认项目中转服务无需填写。
# SANMAO_MEDIA_RELAY_UPLOAD_TOKEN=replace-with-your-relay-upload-token

# 高级自托管备用：Agnes 本地图片、视频和音频输入的公网访问地址；必须是 Agnes 服务器能访问的 HTTPS 地址。
# 未配置时纯文本请求仍可用，媒体请求会明确提示需要中转服务。
# SANMAO_PUBLIC_BASE_URL=https://your-public-sanmao.example.com

# Agnes 签名媒体 URL 的密钥；未设置时使用 SANMAO_MASTER_KEY 或自动生成本地密钥。
# SANMAO_MEDIA_SIGNING_KEY=your-media-signing-secret

# 可选的视频存储目录；不设置时默认使用 SANMAO_DATA_DIR/videos
# SANMAO_VIDEO_STORAGE_PATH=.data/videos

# GitHub 更新清单地址。上传 GitHub 后填写仓库中 update.json 的 raw 地址。
# 未设置时默认先尝试 GitHub，再自动回退到 jsDelivr 公共镜像。
# SANMAO_UPDATE_MANIFEST_URL=https://raw.githubusercontent.com/sanmao44/sanmao.ai-LuminaAgent/main/update.json

# 额外的更新清单镜像地址，按逗号或换行分隔；GitHub 和内置镜像都失败后尝试。
# SANMAO_UPDATE_MANIFEST_MIRRORS=https://gitee.com/sanmao44/sanmao.ai-LuminaAgent/raw/main/update.json

# 额外的安装包镜像地址，按逗号或换行分隔；会在官方安装包失败后尝试。
# SANMAO_UPDATE_MIRRORS=https://gitee.com/sanmao44/sanmao.ai-LuminaAgent/releases/download/v0.7.0/SANMAO.AI-0.7.0.zip

# 用于代理 GitHub 安装包的前缀列表，按逗号或换行分隔；不设置时内置 ghfast.top 与 ghproxy.net。
# 安装包下载后会强制校验 SHA-256，校验失败不会执行更新。
# SANMAO_UPDATE_GITHUB_PROXIES=https://ghfast.top/,https://ghproxy.net/

# 本地更新开关；默认启用。Docker/公网部署建议关闭，由镜像或运维流程更新。
# SANMAO_DISABLE_LOCAL_UPDATE=1

# 公开部署时建议开启，保护 API 服务商和模型管理页面。
# SANMAO_ADMIN_PASSWORD=your-admin-password

# 局域网共享由“启动 SANMAO.AI - 局域网共享.cmd”自动设置，不需要手动填写。
# SANMAO_NETWORK_MODE=lan

# 联网搜索：AnySearch 为主源，失败或额度耗尽时自动切换百度千帆。
# AnySearch 不配置 Key 也可使用匿名免费额度；配置 Key 可获得更高额度和并发。
# 密钥只从环境变量读取，不写入代码或日志。
# ANYSEARCH_API_KEY=sk_xxxx
# QIANFAN_API_KEY=bk_xxxx
