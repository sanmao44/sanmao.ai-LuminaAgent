# 0.6.6 Agent 短指令生图修复

- 修复“画个猫”“帮我画一个猫”等短指令被对话模型当作普通文字返回的问题。
- 对话模型返回生图 JSON 而不是工具调用时，服务端自动转为已配置图片模型的真实生图请求。

# 0.6.5 图片模型触发与加载体验修复

- 修复直接说“生成一只狗”等自然语言生图请求被当作普通文字回答的问题，正确进入图片工具流程。
- 恢复对话生图加载卡的流光动态，生成过程中状态反馈更清晰。

# 0.6.4 续图与后台更新体验

- 修复对话中无法延续上一张助手生成图的问题，支持直接用“调整视觉风格”“优化构图”等表达触发图片编辑流程。
- 图片助手消息新增 1、2、3 数字方向按钮，直接基于该消息最后一张图片续生成；显式参考图仍优先使用。
- 更新下载、替换和重启改为后台任务，更新弹窗与悬浮进度条持续展示阶段和进度，并优化版本更新动画。

# 0.6.3 Agent 联网与对话体验升级

- 默认联网策略改为智能按需，普通创作和聊天无需等待模型工具规划即可开始流式输出。
- 搜索服务未返回来源时明确反馈，并为支持的模型自动尝试原生联网搜索回退。
- 对话出图增加阶段加载卡、成图优先排序和简短创作建议；历史消息保存实际联网信息。
- 联网模式菜单替换浏览器原生下拉，修复深色界面中白底选项和布局溢出。

# 0.5.9 修复干净环境启动

- 启动器现在检查 TypeScript 与 React 类型依赖，并强制按 `--include=dev` 安装，避免生产环境缺少构建依赖导致更新后无法启动。
- 显式设置 TypeScript `baseUrl`，确保干净构建能解析 `@/...` 路径别名。
- Windows、macOS、Linux 启动器统一覆盖依赖检查。

# 0.5.8 修复 Windows 更新后启动卡住

- 兼容旧启动器使用相对 `node_modules` 路径启动 Next 的情况，避免残留服务锁定 `next-swc` 导致 `npm ci` 报 `EPERM`。
- 启动器和停止脚本统一按 SANMAO 健康接口识别相对路径服务，更新后可可靠释放旧进程并重新监听原端口。
- 统一 Windows、macOS 启动器和应用包显示版本号。

# 0.5.7 更新可靠性与进度提示

- Windows 和 macOS 启动器固定使用 webpack，避免中文内容触发 Turbopack 字符边界崩溃。
- 更新弹窗增加旋转加载指示、滚动进度条和“下载/重启”阶段提示，避免用户误以为更新卡住。
- 延续 `Infinite-Canvas` 的下载源兜底、完整性校验、独立替换和原端口回连方案。
- 更新清单支持 Gitee、OSS 和自有 CDN 等 HTTPS 镜像，并按镜像优先、GitHub 兜底的顺序尝试。

# 0.5.6 Windows 启动器锁定修复

- 修复 Windows 启动器无法识别 `node_modules\\.bin\\..\\next` 实际命令路径的问题。
- 避免重复启动时误执行 `npm ci`，从而锁定 `next-swc` 原生文件并触发 `EPERM`。
- 依赖修复前会先停止当前项目残留的 Next 服务，停止脚本也同步支持该路径形式。

# 0.5.5 长文结果查看与双语视频 Prompt 优化

- 修复长结果预览区域的展开按钮被裁剪的问题；展开后由页面整体滚动查看完整内容。
- 多参考图“一镜到底”生成结果同时提供对应的中文和英文 VIDEO PROMPT。
- 保留原有 Prompt 文案，避免重复增加输入 token。

# 0.5.4 信息架构与创作工作台优化

- 重排 Agent、图片、历史与管理入口，保留视频/音频路线占位并明确显示“即将上线”。
- 生图高级参数默认折叠，历史图片强化继续修改、复用参数和作为参考图等高频操作。
- 新增 QQ 交流群与圆角赞赏码支持入口，优化长消息阅读、生成失败反馈和设置分组导航。
- 角度控制台的 3D 取景状态卡调整到画布左下 HUD 区，避开输出尺寸标签与 Camera Map。
- 改善图片服务商响应兼容、错误处理和桌面/移动端布局。
- 顶部 GitHub 图标可直接打开项目主页，版本号区域继续独立承担检查更新与更新提醒。
- 更新检查改为后台自动执行；版本号点击仅显示当前状态，不再触发手动检查。
- GitHub 更新清单暂时不可达时回退读取本地清单，避免已是最新版本却显示错误状态。

# 0.5.3 顶部版本状态提示

- 顶部 GitHub 版本胶囊在检查成功且没有新版本时显示绿色“已是最新”状态。
- 更新检查中、失败或有新版本时不显示误导性的最新状态。
- 优化版本胶囊在浅色、深色、窄窗口和侧边栏切换下的紧凑布局。

# 0.5.2 完整发布

- 补齐 GitHub Release 漏传的共享模块，确保下载包可以通过测试和生产构建。
- 更新 GitHub Actions 发布校验，发布前自动执行测试与构建。
- 接入更新提示与安全的本地更新入口，更新时保留本地配置、历史记录和图片。

# 0.5.1 构建兼容修复

- 修复 Windows 正式构建时 TypeScript 报错：正则表达式 `s` 标志需要 ES2018+。
- `tsconfig.json` 编译目标由 ES2017 提升到 ES2020。
- Data URL 解析改为 `[\s\S]*` 兼容写法，不再依赖 dotAll 正则标志。
- 不改变 0.5.0 的界面和功能，仅修复构建兼容性。

# 更新记录

## 0.5.1
- 生图结果新增：预览、修改、调整、复用、参考图、收藏、下载、删除。
- 新增本地生成历史（IndexedDB）、搜索、筛选、分页、每页数量、批量选择。
- 智能助手新增参考图上传、拖拽、粘贴和视觉输入。
- Agent 新增 image_edit 工具；有参考图且要求修改时可调图片编辑接口。
- 生图页支持最多 16 张参考图，自动转参考图/编辑模式。
- 模型管理改成“拉取后直接勾选使用”，去掉双开关心智。
- 智能助手和生图页均用自定义下拉菜单显示当前已选择模型。
- 新增自定义图片查看器、编辑侧栏、删除确认框，减少系统默认 UI。
- 亮/暗主题统一使用 SANMAO.AI Design Tokens。
- 图片编辑优先尝试 JSON images 输入，兼容回退 multipart/form-data。
# 0.6.1 更新安装与启动可靠性

- 保留并完善 Windows 更新后的端口回连，升级完成后自动回到原服务端口。
- 更新源增加 GitHub 归档兜底与可信来源校验，下载失败时可自动切换。
- 启动与停止脚本可识别并清理本项目残留的开发/生产服务，减少端口占用和文件锁定。
- 发布新的 GitHub 安装包，并通过应用内更新提醒通知现有用户。
# 0.6.2 角度控制台镜头重建与无硬边输出

- 修复 Roll 后处理造成的斜矩形、透明角和模糊背景拼接；改为旋转后放大裁切填满画幅。
- 3D 构图导引提交时保持水平，Roll 仅在最终结果后处理阶段执行一次。
- 强化角度提示词，要求人物、前景、中景和背景随目标机位重建透视、可见面与遮挡关系。
- 增加大角度模型能力提示，并补充横竖画幅、正负 Roll 和导引水平性的自动化测试。
