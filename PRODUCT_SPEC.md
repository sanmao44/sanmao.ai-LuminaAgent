# SANMAO.AI 0.5 产品结构

核心原则：不是无限画布；是“对话 + 生图 + 图片迭代 + 历史复用”的结构化创作平台。

## 一级入口
- 智能助手：对话模型 + 参考图 + 工具调度
- 生图：直接控制提示词、图片模型、参考图、比例、质量、数量
- 生成历史：分页画廊和图片资产复用
- 模型库：服务商读取模型后直接勾选使用
- 接口服务商：多连接管理

## 图片生命周期
生成 → 历史 → 预览 → 修改 / 调整 → 新版本 → 复用 / 参考图 → 下载 / 收藏 / 删除

## 模型生命周期
选择服务商平台 → 填写必要的地址和 Key → 系统自动适配协议并测试 → 拉取真实模型 → 自动识别类型 → 用户纠正类型 → 勾选使用 → 下拉菜单立即可用

服务商配置面向非技术用户：不展示接口路径、鉴权头、协议切换等高级字段。兼容参数由平台模板和后端规则维护，用户只负责选择、填写、测试。

## Agent
Agent 只负责对话、分析和决定是否调用工具。实际图片由图片模型执行。
当前工具：
- image_generate
- image_edit

## 下一阶段建议
- 遮罩编辑器 / 局部重绘
- 任务队列与流式生成中间图
- 对话分支与 @ 引用历史图片
- 收藏夹 / 项目
- 历史 ZIP 导入导出
- PostgreSQL + R2/S3 多用户云端历史
- 用户账户 / 额度 / 计费
